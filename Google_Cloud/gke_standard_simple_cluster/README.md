# GKE Standard Cluster - Simple

The GKE cluster uses the following features:
- Routes-based cluster
- Ingress API to expose the workload
- Observability is enabled (monitoring using Prometheus & logging)